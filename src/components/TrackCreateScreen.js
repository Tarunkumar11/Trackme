import React from 'react'
import { Text, StyleSheet } from 'react-native'

const TrackCreateScreen  = ( ) => {

    return (
        <Text>This is TrackCreateScreen page</Text>
    )
}

const style  = StyleSheet.create({})

export default TrackCreateScreen