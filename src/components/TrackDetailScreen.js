import React from 'react'
import { Text, StyleSheet } from 'react-native'

const TrackDetailScreen  = ( ) => {

    return (
        <Text>This is TrackDetailScreen page</Text>
    )
}

const style  = StyleSheet.create({})

export default TrackDetailScreen